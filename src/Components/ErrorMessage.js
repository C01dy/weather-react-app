import React from "react";

export const ErrorMessage = () => {
    return (
        <h5 className="card-subtitle p-5 text-sm-center text-secondary">
            Ooops. An error occurred while trying to load data.
        </h5>
    )
};